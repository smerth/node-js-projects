# Converting LESS to CSS with Grunt

Install the npm modules
```bash
npm install
```

Covert less to css
```bash
grunt css
```

or 
```bash
grunt
``` 

for the default set of tasks

run the app
```bash
node app
```

